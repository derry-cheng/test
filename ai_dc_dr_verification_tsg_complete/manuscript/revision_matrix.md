# TSG Revision Matrix

## Current follow-up closure (C1--C5)

| Critical issue | Implemented correction | Evidence artifact |
|---|---|---|
| C1: complete-ledger scoring was presented too close to a deployable decision | The paper now labels the complete-ledger panel as mechanism isolation, freezes a slot-60 gate for deployment, masks all post-gate arrivals before target construction, and scores the gate policy separately from the complete-ledger comparator. Execution telemetry is opened only in the locked scoring pass; no utility-event causal claim is made. | Experiment 17 decision-time metadata, `decision_time_comparison.csv`, Table IV, information-boundary audit |
| C2: the risk module was non-identifiable because the reported profile collapsed to the single projection | The reported profile is now the total-plus-daily-CVaR simplex fit without a pointwise cap. Tail-Risk is the CVaR-only ablation, and a separately stored payment-contract profile carries the pointwise activation envelope. A predeclared normalized total-exposure term is added to the convex objective alongside the normalized daily-CVaR term; the locked audit now requires the joint and CVaR-only simplex weights to differ (final (L_\infty) distance (2.95\times10^{-2})). Both risk budgets, KKT residuals, and contract margins are audited independently. | `risk_effect_decomposition.csv`, `risk_module_ablation_weights.csv`, `risk_constrained_validation_certificate.csv`, `two_sided_credit_certificate.csv`, `final_risk_contract_audit.csv`, `test_profiles.npz` |
| C3: the job witness, network replay, and nameplate used incompatible scales | Experiment 21 defines fixed-nameplate and capacity-proportional transforms, and Experiment 22 replays the same indexed witness at the raw and both homogeneous scales. Service energy, GPU cap, site capacity, aggregate profile, and network injection are scaled together; no second aggregate optimization is permitted. | `scale_consistency_summary.csv`, `coupled_network_event_replay.csv`, `coupled_network_scale_replay.csv`, coupling certificate |
| C4: the held-out $q_{99}$ endpoint was treated as both over-capacity and contract eligible | Fixed facility demand is carried separately from the flexible component. Experiment 9 calibrates the payment-network scale on the held-out $q_{99}$ flexible peak, and Experiment 15 solves both raw $q_{01}$ and $q_{99}$ endpoints without clipping. The raw endpoints are activation-eligible in the declared payment model; Experiment 16 reports the independent 118-MW flexible-only capacity-safe factor (0.890086) as a planning diagnostic, not as a payment gate. | Eq. (32), Exp9 conversion metadata, Exp15 endpoint certificates, Exp16 calibration metadata |
| C5: originality and baseline coverage were overstated by presenting standard primitives and structural proxies as independent breakthroughs | The manuscript identifies the original object as the typed ledger-to-settlement coupling invariant and the information-boundary contract. LP, SCED, CVaR, and Shapley primitives are explicitly standard; recent cross-regional dispatchable-capacity and coupled-regulation TSG studies are cited, and their equation-level analogues are evaluated under matched information without claiming software reimplementation. | Proposition 9, `baseline_fairness_audit.csv`, structural baseline summaries, revised Introduction and Table II |
| Follow-up: the coupling residual was not typed and the network replay could hide a second aggregate object | Proposition 9 now specifies the job, aggregation, and region-to-bus residual arrays and their units. Experiment 22 checks all residuals, GPU bounds, and regional capacities before dispatch; Experiment 24 freezes the resulting profiles and evaluates every finite RTS-24 outage without ranking, AC screening, or post-solution reoptimization. | `coupling_invariant_certificate.json`, `all_outage_security_replay.csv`, Figures 26--27 |
| Follow-up: the slot-60 response panel did not contain an independently generated event | Experiment 23 applies a predeclared tariff and service floor to the submission-masked ledger and solves a structurally distinct exact response LP without verifier outputs, risk targets, or candidate profiles. The 54-day controlled replay enforces a nonzero response-policy difference before scoring and reports the contract metrics separately from the mechanism-isolation verifier; the manuscript explicitly excludes a field-causal interpretation. | `independent_event_replay_summary.csv`, `independent_event_profiles.npz`, Figure 26 |
| Follow-up: a complete payment CSV could survive an upstream refit through row-count-only resume logic | Experiment 9 now records SHA-256 digests for the current Experiment-2 test/validation profiles, processed-data manifest, configuration, and certified profile bytes. The audit recomputes these digests and rejects any payment panel whose lineage does not match the current locked inputs. Standalone wrappers also clamp numerical-backend threads before importing SciPy/HiGHS. | Exp9 `experiment_metadata.json`, `payment_evaluation_intervals.csv`, `payment_panel_lineage_matches_current_profiles_and_manifest`, `src/aicdr/__init__.py` |

## Earlier revision history

| Critical issue | Implemented correction | Evidence artifact |
|---|---|---|
| C1: method appeared to be a loose composition of standard LP, CVaR, N--1, and Shapley components | The revised method states the workload-state invariant, the validation-frozen cap profile, the independently selected payment target, and the exact N--1 certificate as one auditable lineage. C1 is supported by train-only scaling, complete ledger provenance, exact convex feasibility, and the explicit distinction between statistical risk output and contractual safe output. | `data_manifest.json`, Experiment 2 risk ablation, Experiment 9 lineage metadata, Experiment 16 provenance certificate |
| C2: public workload and grid sources were not co-located, so causal utility-event identification was overstated | The manuscript now labels the study as a trace-driven benchmark, keeps BurstGPT, MIT/DCGM, and public-grid evidence tiers separate, and states that no causal field intervention is claimed. The 118-MW nameplate is committed before the validation/test split and only reconciled post hoc with the complete same-source MIT/DCGM envelope. | `data/processed/data_manifest.json`, Experiment 13 replay boundary, Experiment 16 capacity reconciliation |
| C3: complete future ledgers were used where a deployment-time decision would have had only partial information | Added a predeclared slot-60 information gate (one hour before the event). The exact verifier is rerun with every post-gate arrival removed, while execution is used only for scoring; the submitted committed-ledger baseline and the post-event DR replay are separate LPs, and the complete-ledger result is retained only as an information upper bound. | Experiment 17 `decision_time_comparison.csv`, information-boundary metadata, Figure 23, audit check |
| Review follow-up: the decision target still came from the complete-day metadata cache | Refit the same metadata gradient-boosting target separately for each locked day after masking the current day's post-gate arrivals before feature construction. The complete-day cache is no longer used for the decision-time target; Experiment 17 now carries schema 11, with explicit submitted-baseline and post-event response roles plus a recorded information-boundary checksum. | `src/aicdr/baselines.py`, `src/aicdr/experiments.py`, Experiments 15/17 metadata/schema, gate information-boundary regression test |
| Review follow-up: an undeclared raw path could bypass the locked manifest | The source validator now records every configured path absent from an existing manifest as a mismatch and aborts before preprocessing. A regression test covers a valid declared file plus an undeclared sibling path. | `src/aicdr/data.py`, `test_preprocessing_rejects_undeclared_raw_manifest_path` |
| C4: the security guarantee was limited to lossless DC N--1 while AC evidence was narrow or ambiguous | Retained the complete preventive DC certificate and IEEE-9 shared-active-plan AC panel, added all IEEE-9/14 corrective AC outages, and added a four-network RTS-24/30/39/118 AC contingency audit. The latter fixes a pre-registered public-bus mapping, freezes non-reference active outputs from the intact AC-OPF, solves every native-case admissible outage with reference-generator/reactive/voltage recourse, and checks apparent-power and voltage limits explicitly. The scope is stated as steady-state fixed-active-plan feasibility rather than transient stability or unit commitment. | Experiments 8, 9, 10, and 18; AC metadata scope; Figures 15 and 24 |
| C5: selected baselines and the headline trace gain could be mistaken for end-to-end verifier superiority | The baseline ledger contains high-recall, statistical, metadata, quantile, feasible-projection, tail-risk, and risk-safe methods under the same information protocol. The trace-anchored RTS-24 gain is explicitly separated from end-to-end results: the updated N--1 means are 139.221/2.208/0.990 USD for trace-anchored base/linear/exact and 167.367/150.461/150.955 USD for the complete workload verifier, with the latter differences reported as non-significant. The validation-frozen payment target, decision-time gap, independent 40-segment scoring, and a two-factor transfer panel absent from certificate selection remain separate evidence layers. | `main.tex`, Experiments 2, 8--12, 17; `payment_target_selection_validation.csv`; `payment_evaluation_unseen_summary.csv` |
| Circular verifier-generated truth | Locked baseline scores use the independently measured BurstGPT/MIT execution tensor; event-response LP outputs remain mechanism-isolation replays and are never labelled as a utility intervention | `data_manifest.json`, Experiment 2 metadata, Experiment 20 |
| Execution power misused as arrivals | Scheduler submission creates queue arrivals; DCGM execution independently creates observed power | `workload_15min.npz`, `data/processed/data_manifest.json` |
| Multi-source data flow was difficult to audit | Added a source-to-join-to-split accounting table with raw, retained, training, held-out, and final valid-slot counts; hashes remain tied to every raw file | `data_flow_audit.csv`, `data_manifest.json`, independent audit |
| Unequal method information | A complete-ledger ex-post learner, selected single projection, tail-risk counterfactual, and risk-safe verifier share the same post-event decision time and never observe execution truth | Experiment 2 information-set audit, profiles, and paired tests |
| Midnight truncation and reset cross-day state | Replaced the zero-arrival completion buffer in the final certificate with a 1,216-slot continuous real-arrival horizon. A lexicographic pair first minimizes submitted event-window distance and then operating cost; pre-event and recovery operation are endogenous, while the terminal state completes all in-window due work and all event-day arrivals without forcing later batch jobs | Experiment 12 rolling profiles, energy residuals, terminal-state unit tests |
| Weak baselines | High-5-of-10, Ridge, histogram gradient boosting, Extra Trees, causal metadata gradient boosting, complete-ledger ex-post gradient boosting and quantile boosting, an exact feasible projection of that quantile estimate, and a selected single feasible projection | Experiment 2 |
| Post-hoc weight exclusion | All six exact projections remain in the total-plus-tail-risk convex fit; a second exact LP imposes the independent single projection as a pointwise event envelope, proving false-credit-MWh noninferiority without test labels | Nested validation, CVaR certificate, risk-envelope validation, convex weights, and metadata |
| IID inference on adjacent days | 54 locked test days, four bootstrap block lengths, and complete nonoverlapping three-day block-sign randomization over 18 blocks with Holm correction | Experiment 2 and audit |
| Price-scale-confounded settlement | Event-specific matched prices and orthogonal gross/net, uniform/nodal, linear/exact mechanisms | Experiment 3 |
| Exact production-cost difference was presented as if automatically an ISO payment | Separated the dispatch-value benchmark, complete-cycle virtual-link remuneration, predeclared DR capacity product, and bilateral Nash contract. The contract uses a continuous no-event optimum for opportunity cost, a no-activation outside option, and explicit individual-rationality and budget-balance equations | Space--time, capacity-product, and Nash-contract equations; Experiment 12 event-only/full-cycle comparison, bilateral utilities, and site allocations |
| Aggregate settlement gain could be driven by one bundled design change | Added locked-day paired contrasts that change signed netting, locational pricing, and exact value one factor at a time, with exact temporal block tests | `settlement_factor_decomposition.csv` and its summary/figure |
| Constructed congestion and single-network evidence | Four public networks at three load levels, fixed buses, fixed 6% penetration, and unmodified public thermal ratings | Experiment 5 |
| Nonbinding physical ablations | Complete 5-by-3 capacity/deadline factorial with explicit binding shares | Experiment 6 |
| One-day imposed manipulation | Ten reference days under the analytically derived expected-settlement coefficient | Experiment 1 and Proposition 1 |
| Manipulation theory tested by its own realized profit | Independent right-hand-side dual marginals from ten honest scheduling programs define the threshold before the strategic programs are evaluated | Experiment 1 dual-certificate metadata, synthetic shadow-price unit test |
| Ten-day reward and cost scales mixed | Expected reward uses the arithmetic-mean baseline, whereas all ten incurred reference-day cost increments are summed; equality is separated from strict profit | Experiment 1 profit columns and Proposition 1 derivation |
| Tautological aggregate value settlement | Market payments use a 10-segment public quadratic cost model and are scored by an independently re-solved 80-segment model; exact allocation uses all 16 primary coalitions and all 256 coalitions in the eight-participant scaling panel | Experiments 3 and 7, Propositions 3 and 4 |
| Settlement conclusions based only on average plots | Complete three-day paired block-sign tests compare all mechanisms in Experiment 3 and signed linear versus exact value in every Experiment 5 network/loading cell, with family-wise Holm correction. A complete 54-day 10/40--80/320 segment-resolution panel verifies convergence at the near-affine boundary cell. | `paired_settlement_block_tests.csv`, `paired_network_block_tests.csv`, `resolution_convergence_daily.csv`, `resolution_convergence_summary.csv` |
| One optimized intervention and complete-telemetry assumption | Four independently solved tariff/participation interventions plus 5%, 10%, and 20% ledger under-coverage stresses | `intervention_robustness.csv`, `specification_robustness.csv` |
| Payment certificate selected the risk target directly or used a weak reference | Candidate hull contains six independently solved first-stage workload projections plus the matched feasible-quantile comparator; the risk-safe profile remains an external target, while the validation-selected single feasible projection defines the payment cap and the quantile profile remains an external transfer comparator | Experiment 9 candidate weights, profile checksum, and metadata |
| Payment certificate assumed one trace-to-power conversion or arbitrary LP tie-breaking | A lexicographic pair of global programs first minimizes target deviation and then maximizes the worst fractional cost margin while embedding complete N--1 dispatch for the held-out 1st, 10th, 50th, 90th, and 99th percentile energy scenarios | Experiment 9 scenario certificates, independent interval evaluations, data manifest, and Proposition 4 |
| Payment-certified verifier was compared only with the matched feasible-quantile reference in prose | Added a same-day complete-response comparison against feasible quantile, selected single projection, and risk-constrained verifier, with exact temporal block tests and all raw day-level outcomes; the contract cap is now explicitly separated from the quantile transfer comparator | Experiment 12 rolling market summary and paired payment comparisons |
| Small closest-comparator gains were reported without effect intervals | Added paired mean effects, relative improvements, three-day moving-block 95% intervals, exact block-sign tests, and metric-wise Holm correction for all seven matched comparators | `matched_comparator_effects.csv`; nRMSE and false-credit interval audit checks |
| Fixed spatial mapping and one power scale | Exhaustively crossed all 24 trace-to-bus mappings with 3%, 6%, and 9% peak penetration and all locked days; no outcome-based placement screening | Experiment 11 |
| AC evidence omitted line outages | Added every finite non-islanding IEEE-9 and IEEE-14 outage as complete nonlinear post-contingency OPF panels over all locked days and four counterfactuals | Experiment 10 AC N--1 tables and figure |
| AC outage evidence allowed a different active dispatch in every outage | Added a complete shared-active-plan preventive panel: the intact non-reference active outputs are equality-fixed in all six IEEE-9 outages, crossed with 3%, 6%, and 9% penetration; only loss balancing, reactive generation, and voltage are recourse | Experiment 10 preventive tables, stored active plans, figure, and exact cross-outage consistency audit |
| Generator output and nodal-value subgradient used the same symbol | Generator output remains \(g\); nodal demand-value subgradients are consistently \(\lambda^0,\lambda^1\); the facility-to-bus incidence matrix \(M\) is explicit | `main.tex`, `model_formulation.md`, `theoretical_results.md`, and formula-source matrix |
| Equations presented without a defensible source boundary | Every model and formula is labeled as cited standard theory, cited adaptation, study definition, or original proved proposition; the audit verifies all bibliography keys across the source matrix and complete formulation | `formula_source_matrix.md`, `model_formulation.md`, `references.bib`, audit |
| Early-preprint or incomplete bibliographic metadata | Replaced the BurstGPT preprint record with its peer-reviewed KDD 2025 publication and complete author list; added the missing volume, issue, and pages for the TSG baseline-manipulation paper and pages for the MIT Supercloud proceedings paper | `references.bib`, compiled references, bibliography audit |
| Standalone diagnostic stages could overwrite the canonical all-stage manifest | The unified entry now reserves `artifacts/run_manifest.json` for `--stage all`; every standalone stage writes under `artifacts/stage_runs/`. A regression test and an independent audit check enforce this evidence-preservation invariant | `pipeline.py`, `test_stage_runs_cannot_overwrite_the_unified_manifest`, `canonical_unified_manifest_preserved` |
| Locked test tail lacked the declared 512-slot future observation window | Restricted eligibility before selection so every validation and test day has at least 40 historical days and the complete maximum-deadline future window; no zero filling or wraparound is permitted | `_event_days`, locked-window unit test, Experiment 12 metadata |

All rows are executed from the unified entry point. A correction is considered
complete only when its final panel, checkpoint, figure, and audit check are present.
