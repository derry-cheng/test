# Paper-to-Implementation Alignment

| Paper claim | Implementation | Direct evidence |
|---|---|---|
| Multi-day baseline rules create endogenous manipulation incentives | `run_exp1`, `mode="strategic_reference"`, independent minimum-service dual certificates | Complete 8-price × 7-call-probability grid over ten reference days; 56/56 strict-profit boundary agreement |
| Workload-state constraints verify a counterfactual independently of its generator | cumulative-state LP; total-plus-daily-tail risk convex counterfactual; exact pointwise event risk envelope | Twelve-method, 54-day Experiment 2 panel including complete-ledger quantile boosting, its exact feasible projection, and synthetic control |
| Physical feasibility—not privileged information—drives the gain | complete submitted-job ledger supplied to ex-post Metadata Gradient Boosting, the selected single projection, tail-risk counterfactual, and risk-safe verifier at the same decision time | Information-set audit, matched comparisons, independent trace-meter replay, and deterministic false-credit-MWh noninferiority certificate |
| Cross-midnight workload is conserved | 1,216-slot continuous real-arrival horizon with deadline-indexed terminal equality, lexicographic event-window embedding, and endogenous pre-event/recovery operation | Experiment 12 energy residuals, conservation audit, and unit tests |
| False credit and missed credit are distinct | `response_metrics` | Precision, recall, \(F_1\), false-credit, and under-credit intervals |
| Gross forecast credit is not cash payment | `response_metrics` and `run_exp17` settlement fields | `paid_response_mwh` is retained as gross forecast credit; `contract_capped_response_mwh` is the observable intersection of submitted credit, frozen contract credit, and the closed meter; `meter_capped_false_response_mwh` and `meter_capped_underpayment_mwh` are oracle-only diagnostics and are never hard-coded to zero |
| Aggregate flow is not a task-level nonpreemptive claim | `run_exp14` immutable-ledger replay; `run_exp19` exact contiguous start-time witness | `job_interval_witness_summary.csv` checks measured contiguous intervals, runtime, GPU count, release, deadline, and native power; the independent job-indexed counterfactual uses every valid scheduler submission, scheduler allocation runtime, a precommitted 96-slot queue allowance, the 128-slot unlimited-runtime rule, and a 0.001-MW/GPU cap |
| Signed nodal accounting separates rebound from relief | five matched-price mechanisms in `run_exp3` | Oracle mechanism isolation, complete end-to-end factorial, paired block tests, and locked-day one-factor decomposition of signed netting, locational pricing, and exact valuation |
| Market remuneration includes anticipatory shifting and delayed recovery | complete-cycle nodal price integral and exact site decomposition in `run_exp12` | Event-window settlement error versus the complete-cycle settlement-value residual, post-event rebound, zero cycle-energy residual, site budget balance, and three paired payment-verifier comparisons |
| Exact signed net value is the SCED value-function difference | 10-segment market settlement and independently re-solved 80-segment evaluator using aligned public IEEE-118 quadratic costs | Nonzero trace-reference error prevents circular self-scoring; nodal linear versus exact avoided-cost results |
| Conclusions are not tied to IEEE 118 or constructed line limits | `power_system_from_ppc`, `run_exp5` | Four native-rating networks × three load levels × two baseline qualities, plus cellwise paired mechanism inference |
| Deadline, capacity, telemetry coverage, and intervention modules have observable effect | `run_exp2`, `run_exp6` | 5×3×54 physical stress panel, 5/10/20% ledger under-coverage, and four independently solved intervention specifications |
| Multi-participant net value is allocated efficiently | exact coalition enumeration plus binomial count-state Shapley summation in `run_exp7` | Primary 16-coalition panel, all 256 coalitions for eight participants, and exact 4--20 participant scaling on every locked day |
| Signed net-value settlement remains aligned under line-outage security | complete finite LODF constraints in `solve_n1_sced`; `run_exp8` | All 37 non-islanding RTS-24 line outages, 54 locked days, eight event intervals, separate 40-segment N--1 evaluator, and paired block tests |
| The workload verifier has a direct payment-level guarantee robust to measured power conversion | `solve_payment_certified_n1_projection`; `run_exp9` | 54 lexicographic LP pairs choosing over six independent first-stage projections plus the matched feasible-quantile comparator and embedding every N--1 dispatch row for held-out 1st/10th/50th/90th/99th-percentile conversion scenarios; the contractual cap is anchored to the validation-selected single projection, the quantile profile is an external transfer comparator, and the risk verifier is not a selectable candidate |
| Main findings are checked outside the lossless DC model | nonlinear PYPOWER AC OPF in `run_exp10` and `run_exp18` | Four public base-state networks, every finite non-islanding IEEE-9/14 corrective outage, and a complete RTS-24/IEEE-30/39/118 shared-active-plan preventive panel at 3%, 6%, and 9% penetration under the 0.90 native-load multiplier |
| Deployment-time information is separated from the complete-ledger upper bound | `run_exp17` masks every post-slot-60 arrival before solving two exact LPs: a gate-consistent submitted baseline and a declared-DR-price rolling response on the same committed ledger; the event response is an operating replay, while measured execution is scored in a separate observational panel | 54 paired locked-day records, explicit terminal checkpoint and objective metadata, future-arrival audit, Experiment 20 trace replay, and Figure 23 |
| Cross-network AC transfer is auditable without an invalid preventive guarantee | `run_exp18` fixes six day/slot snapshots, the topology-based admissible outage set, generator-bus electrical-role mapping, and validation-only scale before solving an intact AC-OPF and fixed-active-plan contingency AC-OPFs | Complete RTS-24/30/39/118 outage records across six snapshots with loading, voltage, and active-plan-deviation diagnostics; metadata records the preventive fixed-active-plan scope, mapping basis, and hard AC limits |
| Fixed geography and trace-to-power scaling do not select the reported result | complete permutation and scale enumeration in `run_exp11` | All 24 regional-trace-to-bus mappings plus two predeclared concentration controls × three penetrations × four counterfactuals × all 54 locked days, with independent settlement and truth resolutions |
| Statistical inference respects serial dependence | moving-block bootstrap, block-length sensitivity, and complete nonoverlapping block-sign randomization with Holm correction | 5,000-replication intervals, 18 three-day blocks, \(2^{18}\) exact sign assignments, and family-wise corrected paired tests |

| Independent execution is not an event label | `run_trace_meter_replay` scores locked profiles against the measured DCGM execution tensor without event-response optimization | 648 daily method scores, three-day block intervals, and an explicit `event_intervention=false` metadata field |
| Indexed workload and network settlement are one typed object | `run_exp19` stores the service witness; `run_exp22` validates job, aggregation, and bus-mapping residuals before SCED | 13,198,247 admissible starts over 75,326 submissions, zero residuals, GPU/site-capacity checks, and `coupling_invariant_certificate.json` |
| A deployment gate is tested with an independently generated event | `run_exp23_independent_event_replay` applies a predeclared tariff and service floor to the submission-masked ledger and solves a structurally distinct response LP | 54 locked days, no verifier/risk target input, nonzero policy-trajectory separation checked before scoring, and separate contract-delivery metrics |
| Full DC security coverage is distinct from native-case AC admissibility | `run_exp24_all_outage_security_panel` freezes profiles and evaluates every finite RTS-24 outage | 864 profile cells, 37 outages per cell, 31,968 contingency solves, and no outage ranking or post-solve profile reoptimization |

All workload and SCED optimization problems are solved to HiGHS optimality. The
risk ensemble uses a full linear epigraph and trust-region KKT certificate; no
feasibility repair, rule-based dispatch, mock row, or result substitution is used.
The statistical estimators are comparators and pre-estimators, not hidden generators
of the trace-observed scoring truth.
