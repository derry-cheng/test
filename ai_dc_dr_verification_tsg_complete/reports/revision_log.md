# Revision and verification log

The current branch addresses the five critical review points with code, regenerated artifacts, and manuscript edits.

| Review point | Implemented correction | Evidence |
|---|---|---|
| C1: runtime/waiting-cost inconsistency | The contiguous-start selector evaluates the full block-delay integral (R d+R(R-1)/2), including runtime in every waiting-cost term. The one-job brute-force check and all runtime certificates use the same expression. | `code/src/aicdr/executable_witness.py`; `experiments/exp27_executable_common_witness/results/final/runtime_witness_coupling_certificate.json` |
| C2: disconnected aggregate and job views | The locked Exp2 risk profile is retained, converted once to a submit-time declaration-energy-scaled executable contract, and mapped to exact finite start enumeration. Raw/scaled profile digests, scales, daily residuals, and the common declaration digest are checked by Exp26. | `risk_to_executable_bridge.csv`, `risk_to_executable_bridge_certificate.json`, Exp26 lineage certificate |
| C3: nameplate energy confused with physical energy | Exp27 stores the conservative GPU-nameplate upper-capacity profile and calibrated central declared-energy profile on identical starts, with separate typed certificates and residuals. Network valuation uses the declared upper-capacity profile and reports the central profile explicitly. | `runtime_complete_witness.npz`, `common_witness_summary.csv`, manuscript indexed-workload subsection |
| C4: non-independent replay and weak tail audit | Exp23 now contains an independent scalar start enumerator, duplicate-pass self-checks, and a seven-row protocol certificate. Exp28 is rerun from the locked Exp2 risk outputs with paired bootstrap tail metrics. | `experiments/exp23_independent_event_replay/results/final/independent_event_protocol_certificate.csv`; Exp28 final outputs |
| C5: event-only/clipped settlement | Exp27 replays all 54 locked days × 96 slots, evaluates every finite N−1 contingency, preserves signed network value and settlement (including debits), and checks meter-credit equality without nonnegative clipping. | `common_witness_settlement.csv`, `common_witness_settlement_summary.csv`, Exp26 lineage certificate |

The final refit reruns Exp2, Exp9, Exp10, Exp17, Exp20, Exp23, Exp26, Exp27, and Exp28. Exp9 uses checkpointed high-resolution N--1 evaluation with the current profile and configuration digests; Exp10 contains 864 connected-base AC-OPF rows, 5,400 corrective AC N--1 rows, and 3,888 shared-active-plan preventive rows, all feasible with zero voltage violation and zero fixed-plan deviation. Exp17 now selects eta=0.55 from four predeclared reserve candidates under a 1.0-MWh/day validation false-credit allowance; no candidate can silently bypass the budget, and the reserve is excluded from payment eligibility. Exp26 passes with 75,326 indexed jobs and 13,198,247 job--slot variables. The final audit reports 375/375 checks passed, the deterministic regression suite passes, and the IEEE journal PDF compiles to 10 pages with a 227-word abstract and no overfull or undefined-reference warnings. The checkout remains far below the 4 GB limit; bounded solver/evaluation workers and one-thread BLAS/HiGHS settings keep every stage within the 20-core ceiling. Exp10 records the current profile transition and full AC-panel recomputation in `ac_profile_refit_reuse_manifest.json`.
