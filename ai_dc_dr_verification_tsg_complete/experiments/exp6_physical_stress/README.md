# Experiment 6: Binding physical-constraint stress test

This pre-declared 5-by-3 factorial experiment varies site capacity and workload
deadlines on every locked test day. It reports verification error together with
the empirical share of binding capacity and deadline constraints. All cells are
solved to LP optimality; no feasibility repair or heuristic dispatch is used.

Run with `./run_all.sh --stage exp6`.
