# TSG submission bundle

This directory is the ten-page publication snapshot generated from `manuscript/`.
It contains the self-contained LaTeX source, bibliography, figures, compiled
PDF, and rendered-page QA images. The snapshot was copied only after the final
build passed with no undefined references or overfull boxes. Numerical results
remain authoritative in the experiment-owned `results/final/` folders.
